import 'package:flutter/material.dart';
import '../appbar/style_appbar.dart';

class MyAppBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text('MyAppBar', style: lol),
    );
  }
}
